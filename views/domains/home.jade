extends index

block domains-content
  .container
    br
    table.table.table-striped.table-bordered
      caption
        h3 
          | 我的域名邮箱 &nbsp;
          a.btn.btn-success(href="/domains/addNewDomain") 添加
          span.pull-right 已用额度: xxx/100
        
      thead
        tr
          th.name 域名
          th.cnameVerifyStatus 验证所有权
          th.mx MX记录
          th.forward_email 转发到
          th.operation 操作
      tbody
        for domain in domains
          tr
            th.name #{domain.domain}
            if domain.cname_hadVerify
              th.cnameVerifyStatus #{domain.cnameVerifyStatus}
            else
              th.cnameVerifyStatus 
                | 尚未通过
                a(href="/domains/newDomainSetup?domain=" + domain.domain) 手动验证
            if domain.mx_hadVerify
              th.mx #{domain.mx}
            else
              th.mx
                | 未设置或暂未生效
                a(href="/domains/newDomainSetup?domain=" + domain.domain) 如何设置
            th.forward_email #{domain.email}
              if !domain.email_hadVerify
                a(href="/domains/newDomainSetup?domain=" + domain.domain) 再次发送验证邮件
            th.fee_plan #{domain.fee_plan}
            th.operation 
              a(href="/domains/deleteDomain?domain=" + domain.domain) 删除
                
            
            
  
  //- table.table.table-striped.table-hover
  //-   thead
  //-     tr
  //-       th #
  //-       th 域名
  //-       th 转发策略
  //-       th 收件箱
  //-       th 编辑
  //-   tbody
  //-     for domain, idx in domains
  //-       tr
  //-         td #{idx}
  //-         td #{domain.domain}
  //-         td #{domain.forward_type}
  //-         td #{domain.forward_email || '未设置'}
  //-         td 
  //-           a(href="/domains/setup?domain=" + domain.domain).btn.btn-info.btn-xs 设置            
  //- a(href="/domains/addNewDomain").btn.btn-success 添加新域名
