extends ../../layout

block content

  .container
    .row
      .col-md-offset-3.col-md-6
        form(action="#", method="post")
          input(type='hidden', name='_csrf', value=_csrf)
          p 确定要删除域名 #{domain} 吗?, 删除后不可撤销!!!
          button(type="submit").btn.btn-danger 删除
          a(href="/domains/").btn.btn-success 不删除
