extends ../../layout

block content

  .container
    .row
      .col-md-offset-3.col-md-6
        form(action="#", method="post")
          input(type='hidden', name='_csrf', value=_csrf)
          p #{__("确定要删除域名 %s 吗?, 删除后不可撤销!!!", domain)}
          button(type="submit").btn.btn-danger #{__("删除")}
          a(href="/domains/").btn.btn-success #{__("不删除")}
