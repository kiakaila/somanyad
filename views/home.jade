extends layout

block content
  //- .row
  //-   .col-md-6
  //- .container
  //-   .row

  section#home
    .container
      .row
        .col-md-12
        .col-md-7
          p.lead #{__('为了避免收到垃圾邮件')}
          p.lead #{__('您是否把邮件地址图片化')}
          p.lead #{__('您是否替换邮箱地址中的@成为#')}
          p.lead #{__('您是否将邮箱地址通过base64进行转码')}
          p.lead #{__('然而,这依旧没法应对那些被脱裤,甚至主动发广告到你邮箱的网站')}
    .right_bottom.clearfix
      .container
        .row
          .col-md-12.pull-right
            h2 #{__('不得不说')}
          .col-md-12.pull-right
            h1.pull-right #{__('好多广告!!!  ===>  So Many Ad!!!')}
        
  section#price
    .container
      .row
        .col-md-12
          p.lead #{__('但是,如果你有自己的域名')}
          p.lead #{__('你可以为每个网站,每种用途都准备一个邮箱, 然后转发到你的常用邮箱')}
          p.lead #{__('你可以知晓哪个网站给你发广告,或者裤子被拖了(不是很担心黑客的无差别攻击咯~~拽拽的)')}
          
  section#product
    .container
      .row
        .col-md-12
  section#contact
    .container
      .row
        .col-md-12
          
          
          
block footer-js
  != js("lib/jquery-2.1.3.min")
  != js("lib/bootstrap.min")

  != js("freelancer/jquery.easing.1.2")
  != js("freelancer/freelancer")
  
    
